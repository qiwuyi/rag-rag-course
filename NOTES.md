# NOTES.md

## 用户偏好
- 中文讲解，代码/术语保留英文
- 基础：会 JS/TS，了解 RAG / 向量数据库概念（已有基础，不需要从"什么是 embedding"开始）
- 学习方式：HTML 课程 + 测验（默认方式）
- 不部署，只读代码理解原理

## 工作区结构
- 教学对象：`D:\ds\rag-chatbot-fork`（克隆的仓库）
- 教学工作区：`D:\ds\teach-rag-chatbot`
- 课程 HTML 位于 `./lessons/`，速查表在 `./reference/`，共享样式在 `./assets/`

## 下一步建议
- 第二节课后：让用户动手改 `chunkText` 的 maxLen 看效果（可以写个小测试）
- 第三节课后：让用户口述一遍 `/chat` 的完整请求旅程
